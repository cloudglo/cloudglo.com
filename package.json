{
  "name": "cloudglo-website",
  "version": "1.0.0",
  "private": true,
  "description": "CloudGlo Consulting - Professional consulting services",
  "scripts": {
    "dev": "wrangler pages dev --port 3000",
    "deploy": "wrangler pages deploy",
    "deploy:preview": "wrangler pages deploy --env preview"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
